# Core pipeline — runs the full tabular path offline on CPU
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
magika>=0.5            # Ingestion Identifier Agent (paper's file-type detector)
perpetual>=0.4         # Model Inference Agent GBM (paper's PerpetualBooster)
shap>=0.44             # Explainability (SHAP top-10)
pyreadstat>=1.2        # read SPSS .sav files (the HOPE dataset)
openpyxl>=3.1          # read .xlsx files

# --- Production components (paper's cloud/GPU paths) — install with:
#     pip install -e ".[full]"
# transformers>=4.40   # SapBERT 768-d header embeddings (Model-Data Matcher)
# torch>=2.1           # SapBERT + MedGemma + DETR
# lime>=0.2            # LIME explanations (alternative to SHAP)
# google-cloud-dlp     # Data Anonymizer Agent (Google Cloud DLP backend)
# google-adk           # Orchestration with Google's Agent Development Kit
